<Page x:Class="WindowsDebloater.Pages.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      Title="Services">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,30,40,30">
            <!-- Header -->
            <TextBlock Text="Services Optimizer" 
                      FontSize="28" 
                      FontWeight="SemiBold"
                      Margin="0,0,0,10"/>
            <TextBlock Text="Optimize Windows services for better performance" 
                      FontSize="14" 
                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                      Margin="0,0,0,30"/>

            <!-- Warning -->
            <Border Background="#FFE5E5" 
                   BorderBrush="#FF6B6B" 
                   BorderThickness="1" 
                   CornerRadius="4" 
                   Padding="15"
                   Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🛑" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock TextWrapping="Wrap" VerticalAlignment="Center">
                        <Run FontWeight="SemiBold">Critical:</Run>
                        <Run Text=" Disabling essential services may cause system instability. Only disable services you understand."/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Controls -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="Refresh Services" 
                       Click="RefreshButton_Click"
                       Margin="0,0,10,0"
                       Padding="15,8"/>
                <Button Content="Optimize Selected" 
                       Click="OptimizeButton_Click"
                       Margin="0,0,0,0"
                       Padding="15,8"
                       Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>

            <!-- Services List -->
            <Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                   BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                   BorderThickness="1"
                   CornerRadius="4"
                   Padding="0">
                <ListView x:Name="ServicesListView"
                         Height="400"
                         SelectionMode="Extended"
                         BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Service Name" Width="250">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ServiceName}" FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Display Name" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayName}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Status" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Status}" 
                                                  Foreground="{Binding StatusColor}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Recommendation" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Recommendation}" 
                                                  Foreground="{Binding RecommendationColor}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <!-- Status -->
            <TextBlock x:Name="StatusTextBlock" 
                      Margin="0,15,0,0"
                      FontSize="13"
                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
