<Page x:Class="WindowsDebloater.Pages.AdvancedPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      Title="Advanced">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40,30,40,30">
            <TextBlock Text="⚡ Advanced Optimization" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <TextBlock Text="Powerful system tweaks for maximum performance" 
                      FontSize="14" 
                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                      Margin="0,0,0,30"/>

            <!-- Quick Optimizations -->
            <Border Background="{DynamicResource SystemControlBackgroundAccentBrush}"
                   CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="🚀 One-Click Optimizations" FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Apply powerful optimizations instantly" Foreground="White" Opacity="0.9" Margin="0,5,0,15"/>
                    <Button Content="FULL OPTIMIZATION PACK" Click="FullOptimize_Click" 
                           Padding="20,12" FontWeight="Bold" Margin="0,0,0,10"
                           Style="{StaticResource AccentButtonStyle}"/>
                    <TextBlock Text="Applies all optimizations below automatically" 
                              Foreground="White" Opacity="0.8" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Registry Optimizations -->
            <TextBlock Text="Registry Tweaks" FontSize="18" FontWeight="SemiBold" Margin="0,10,0,15"/>
            <StackPanel Margin="0,0,0,20">
                <Button Content="✓ Disable Windows Telemetry" Click="DisableTelemetry_Click" 
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="✓ Disable Cortana" Click="DisableCortana_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="✓ Disable Windows Update (Manual)" Click="DisableWindowsUpdate_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="✓ Disable GameDVR/Game Bar" Click="DisableGameDVR_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="✓ Optimize Visual Effects" Click="OptimizeVisuals_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
            </StackPanel>

            <!-- Performance Tweaks -->
            <TextBlock Text="Performance Tweaks" FontSize="18" FontWeight="SemiBold" Margin="0,10,0,15"/>
            <StackPanel Margin="0,0,0,20">
                <Button Content="⚡ Enable Ultimate Performance Power Plan" Click="UltimatePower_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="⚡ Disable SuperFetch/Prefetch" Click="DisableSuperfetch_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="⚡ Optimize Network Settings" Click="OptimizeNetwork_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="⚡ Clear All Temp Files &amp; Cache" Click="ClearAllTemp_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="⚡ Optimize SSD (TRIM)" Click="OptimizeSSD_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
            </StackPanel>

            <!-- Privacy -->
            <TextBlock Text="Privacy &amp; Security" FontSize="18" FontWeight="SemiBold" Margin="0,10,0,15"/>
            <StackPanel Margin="0,0,0,20">
                <Button Content="🔒 Disable Location Tracking" Click="DisableLocation_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="🔒 Disable Activity History" Click="DisableActivityHistory_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="🔒 Block Microsoft Telemetry IPs" Click="BlockTelemetryIPs_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
                <Button Content="🔒 Disable Advertising ID" Click="DisableAdID_Click"
                       HorizontalAlignment="Stretch" Padding="15,10" Margin="0,5"/>
            </StackPanel>

            <!-- Status -->
            <Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                   BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                   BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,10,0,0">
                <TextBlock x:Name="StatusTextBlock" 
                          Text="Ready to optimize" 
                          TextWrapping="Wrap"/>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
